services:
  - type: web
    name: aqt-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn aqt_api:app --bind 0.0.0.0:$PORT --timeout 600 --workers 1
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
